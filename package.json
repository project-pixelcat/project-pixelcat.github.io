{
  "name": "pixelcat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp build",
    "dev": "gulp",
    "clean": "rm -rf build",
    "deploy": "npm run clean && npm run build && gh-pages -d build -b master"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "gh-pages": "^0.11.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-copy": "0.0.2",
    "gulp-cssnano": "^2.1.1",
    "gulp-jade": "^1.1.0",
    "gulp-postcss": "^6.1.0",
    "gulp-serve": "^1.2.0",
    "postcss-cssnext": "^2.5.2",
    "pug": "^2.0.0-alpha3"
  },
  "babel": {
    "presets": ["es2015", "stage-0"]
  }
}
